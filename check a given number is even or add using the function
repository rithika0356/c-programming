#include <stdio.h>
int main (){
  int num; 
  printf f ("enter an integer:");
  scanf ("%d ",&num);
  // true if num is perfectly divisible by 2
   if (num %2==0)
   printf("% d is even .",num);
   else
   printf ("%d is add.",num);
   return 0;
